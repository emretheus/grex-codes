---
"grex": patch
---

Update the bundled coding agents and their driver SDKs — Claude Code, Codex, Cursor, OpenCode, and Kimi — along with the bundled `gh`, `glab`, `cloudflared`, `llama.cpp`, and Node runtime, to their latest versions.
